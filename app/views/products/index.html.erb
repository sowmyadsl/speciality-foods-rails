<h1>Products</h1>

<div class="flex-box">
     <% @products.each do |product| %>
     <div class="product-img">
      <h3><%= product.name %></h3>
       <%= link_to image_tag(product.image), product_path(product)%>
     </div>
     <% end %>
 </div>
